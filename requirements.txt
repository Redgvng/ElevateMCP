# Core MCP framework
mcp>=1.27.0

# Used by browser-inspect, hermes-bridge, package-info, and others for HTTP
httpx>=0.28.0
requests>=2.32.0

# Used by time MCP
croniter>=2.0.0

# Used by json-query MCP
jsonpath-ng>=1.7.0

# Optional (prompted during install) — uncomment if you want them by default:
# fastembed>=0.8.0
# numpy>=2.0.0
# psycopg[binary]>=3.2.0
# pymysql>=1.1.0
# playwright>=1.49.0
